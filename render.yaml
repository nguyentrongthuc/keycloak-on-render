services:
  - type: web
    name: keycloak
    env: docker
    plan: free
    buildCommand: ./bin/kc.sh build
    startCommand: ./bin/kc.sh start --optimized --http-enabled=true --hostname-strict=false
    envVars:
      - key: KEYCLOAK_ADMIN
        value: admin
      - key: KEYCLOAK_ADMIN_PASSWORD
        value: 1PUAU07BABNkB3rmItclusfwUxRTXsIa
      - key: KC_DB
        value: postgres
      - key: KC_DB_URL
        value: jdbc:postgresql://dpg-d431shbipnbc73c2mgig-a/keycloak_db_vfzf
      - key: KC_DB_USERNAME
        value: keycloak_db_vfzf_user
      - key: KC_DB_PASSWORD
        value: (mật khẩu thật của bạn)
      - key: KC_HTTP_PORT
        value: 8080

databases:
  - name: keycloak-db
    plan: free
